EASY

Description
The addMove() function manages game history and truncates future moves if adding a move in the past, but it lacks unit tests.

Location
chess-analysis-app/src/core/game_record.hpp:13

Rationale
The addMove function logic is self-contained. Adding a test requires instantiating GameRecord, adding a few dummy moves, changing the currentIndex, and adding a new move to verify truncation. This is simple state verification.

Code context
    GameRecord() : currentIndex(0) {}

    void addMove(Move m, const std::string& san = "") {
        // If we differ from history (we are in past), truncate future
        if (currentIndex < moves.size()) {
            moves.resize(currentIndex);
            sanMoves.resize(currentIndex);
        }
        moves.push_back(m);
        sanMoves.push_back(san);
        currentIndex++;
    }

Description
The isCheckmate() function checks if the current side is in checkmate, but lacks dedicated unit tests.

Location
chess-analysis-app/src/core/board.hpp:23

Rationale
Testing this simply requires setting up a checkmate FEN and asserting the result is true. It can easily be done in under 10 lines of code using the existing testing macros.

Code context
    Piece getPiece(Square s) const;
    Side getTurn() const;
    bool isCheck() const;

    // Inlined for linking
    bool isCheckmate() const {
        return isCheck() && getLegalMoves().empty();
    }

    bool isStalemate() const {
        return !isCheck() && getLegalMoves().empty();
    }

Description
Calling getLegalMoves().empty() forces the allocation of multiple std::vector objects and validates all pseudo-legal moves, instead of returning true early on the first found legal move.

Location
chess-analysis-app/src/core/board.hpp:24

Rationale
We can add a `hasLegalMoves()` function that returns `true` as soon as `copy.makeMove(m)` is valid, avoiding full generation and allocation of vectors. The change is simple and self-contained within board class.

Code context
    // Inlined for linking
    bool isCheckmate() const {
        return isCheck() && getLegalMoves().empty();
    }

    bool isStalemate() const {
        return !isCheck() && getLegalMoves().empty();
    }

Description
The variables hChildStd_IN_Rd and hChildStd_OUT_Wr are declared in StockfishClient but never used.

Location
chess-analysis-app/src/engine/stockfish.hpp:38

Rationale
A search for 'hChildStd_IN_Rd' and 'hChildStd_OUT_Wr' shows they are only declared in the header file and never used in the implementation file.

Code context
    EvalCallback onEval;
    std::mutex callbackMutex;

    // Use void* to avoid including windows.h in header (Raylib conflict)
    void* hChildStd_IN_Rd = nullptr;
    void* hChildStd_IN_Wr = nullptr;
    void* hChildStd_OUT_Rd = nullptr;
    void* hChildStd_OUT_Wr = nullptr;
    void* hProcess = nullptr;

    void readOutputLoop();

Description
The inline function moveToCoord is defined but never used in the codebase.

Location
chess-analysis-app/src/core/move_utils.hpp:7

Rationale
A search for 'moveToCoord' reveals no usages other than its definition and a comment mentioning it. It can be safely removed or kept if intended for future use, but currently it is dead code.

Code context
#pragma once
#include "types.hpp"
#include <string>

namespace Chess {

inline std::string moveToCoord(const Move& m) {
    std::string s = squareToString(m.from) + squareToString(m.dest);
    if (m.promotion != NO_PIECE_TYPE) {
        char p;
        switch(m.promotion) {
            case QUEEN: p = 'q'; break;
            case ROOK: p = 'r'; break;
            case BISHOP: p = 'b'; break;
            case KNIGHT: p = 'n'; break;
            default: p = 'q'; break;
        }
        s += p;
    }
    return s;
}

}

Description
The function isWrap is defined but never called, representing dead code that can be removed.

Location
chess-analysis-app/src/core/move_gen.cpp:21

Rationale
The function 'isWrap' is completely unused in the entire codebase based on a text search, making it safe to delete.

Code context
// Check if moving 'step' from 'current' crosses board edge
// For 1D representation, we need to be careful about file wrapping.
// We can check if file index changes by more than 1 or wraps.
bool isWrap(int from, int destSq) {
    int f1 = from % 8;
    int f2 = destSq % 8;
    // If we moved horizontally/diagonally, file diff should be small
    // Actually, simpler approach:
    // Precompute distances to edge for every square/direction? 
    // Or just robust coordinate math.
    int r1 = from / 8;
    int r2 = destSq / 8;
    int dr = r2 - r1;
    int df = f2 - f1;
    // If it's a valid sliding move, abs(dr) == abs(df) (diag) or one is 0 (straight)
    if (abs(dr) == abs(df)) return false; // Diag OK
    if (dr == 0 || df == 0) return false; // Straight OK
    return true; // Invalid jump (wrap)
}

Description
The method getEval is declared and defined in GameRecord but never used.

Location
chess-analysis-app/src/core/game_record.hpp:63

Rationale
A search for 'getEval' shows it is never used anywhere in the codebase. It can be safely removed.

Code context
    // Store eval
    void setEval(int moveIndex, const std::string& eval) {
        evaluations[moveIndex] = eval;
    }
    
    std::string getEval(int moveIndex) const {
        auto it = evaluations.find(moveIndex);
        if (it != evaluations.end()) return it->second;
        return "";
    }

    // Public members for direct access if needed, or keeping simple


MEDIUM
Description
The loadPgn() function in the Board class parses PGN strings to recreate a board state but has no dedicated unit tests.

Location
chess-analysis-app/src/core/board.hpp:646

Rationale
Testing loadPgn requires passing a valid PGN string and verifying the final board FEN or piece positions. While straightforward, it requires a bit more setup for test fixtures compared to simple boolean checks.

Code context
            if (cleanMy == cleanIn) return m;
        }
        return {}; 
    }

    inline void Board::loadPgn(const std::string& pgn) {
        reset();
        std::string cleanPgn = pgn;
        for(char& c : cleanPgn) if (c == '\n' || c == '\r') c = ' ';
        
        std::string movesOnly;

Description
moveToSan calls getLegalMoves() to get all valid moves on the board just to check if disambiguation is required for a single move, allocating vectors and checking king safety repeatedly.

Location
chess-analysis-app/src/core/board.hpp:563

Rationale
Disambiguation logic could just use pseudo-legal moves and verify if those ambiguous moves are actually legal, rather than generating all legal moves globally. Still requires a moderate refactor of how ambiguous moves are filtered.

Code context
        }
        
        // Disambiguation
        std::vector<Move> moves = getLegalMoves();
        bool anySameFile = false;
        bool anySameRank = false; // "rank" means row here (0-7)
        bool ambiguous = false;

        for (const auto& other : moves) {
            if (other.from == m.from) continue;

Description
The StockfishClient engine integration class is entirely untested, including the start() method that initializes the background process.

Location
chess-analysis-app/src/engine/stockfish.hpp:15

Rationale
Testing this requires integration with a separate executable (stockfish.exe) or creating mocks for Win32 pipe interactions. This requires significant setup and structural changes to the test harness.

Code context
class StockfishClient {
public:
    StockfishClient(const std::string& path = "stockfish.exe");
    ~StockfishClient();

    bool start();
    void stop();
    
    // Non-blocking commands
    void sendCommand(const std::string& cmd);
    void setPosition(const std::string& fen, const std::vector<std::string>& moves = {});
